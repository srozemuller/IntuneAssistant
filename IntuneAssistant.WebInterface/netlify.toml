[build]
  command = "npm install && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"

# Handle client-side routing (order matters)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
